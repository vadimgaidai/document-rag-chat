/** Chunks embedded and written as one group, so a long document writes as it goes. */
export const EMBED_GROUP_SIZE = 25
export const EMBED_CONCURRENCY = 8

/** Joins the heading breadcrumb that is prepended to a chunk before embedding. */
export const HEADING_SEPARATOR = " › "
