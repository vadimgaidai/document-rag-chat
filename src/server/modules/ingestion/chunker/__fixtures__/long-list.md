# A list that must never be split

A short paragraph before the list.

- Item 01: a list entry long enough that the whole list passes the chunk target on its own
- Item 02: a list entry long enough that the whole list passes the chunk target on its own
- Item 03: a list entry long enough that the whole list passes the chunk target on its own
- Item 04: a list entry long enough that the whole list passes the chunk target on its own
- Item 05: a list entry long enough that the whole list passes the chunk target on its own
- Item 06: a list entry long enough that the whole list passes the chunk target on its own
- Item 07: a list entry long enough that the whole list passes the chunk target on its own
- Item 08: a list entry long enough that the whole list passes the chunk target on its own
- Item 09: a list entry long enough that the whole list passes the chunk target on its own
- Item 10: a list entry long enough that the whole list passes the chunk target on its own
- Item 11: a list entry long enough that the whole list passes the chunk target on its own
- Item 12: a list entry long enough that the whole list passes the chunk target on its own
- Item 13: a list entry long enough that the whole list passes the chunk target on its own
- Item 14: a list entry long enough that the whole list passes the chunk target on its own
- Item 15: a list entry long enough that the whole list passes the chunk target on its own
- Item 16: a list entry long enough that the whole list passes the chunk target on its own
- Item 17: a list entry long enough that the whole list passes the chunk target on its own
- Item 18: a list entry long enough that the whole list passes the chunk target on its own
- Item 19: a list entry long enough that the whole list passes the chunk target on its own
- Item 20: a list entry long enough that the whole list passes the chunk target on its own
- Item 21: a list entry long enough that the whole list passes the chunk target on its own
- Item 22: a list entry long enough that the whole list passes the chunk target on its own
- Item 23: a list entry long enough that the whole list passes the chunk target on its own
- Item 24: a list entry long enough that the whole list passes the chunk target on its own
- Item 25: a list entry long enough that the whole list passes the chunk target on its own
- Item 26: a list entry long enough that the whole list passes the chunk target on its own
- Item 27: a list entry long enough that the whole list passes the chunk target on its own
- Item 28: a list entry long enough that the whole list passes the chunk target on its own
- Item 29: a list entry long enough that the whole list passes the chunk target on its own
- Item 30: a list entry long enough that the whole list passes the chunk target on its own
- Item 31: a list entry long enough that the whole list passes the chunk target on its own
- Item 32: a list entry long enough that the whole list passes the chunk target on its own
- Item 33: a list entry long enough that the whole list passes the chunk target on its own
- Item 34: a list entry long enough that the whole list passes the chunk target on its own

A short paragraph after the list.
