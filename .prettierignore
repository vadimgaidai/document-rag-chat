node_modules
dist
.output
.nitro
.tanstack
.claude

# Vendored third-party agent skills — pinned by skills-lock.json, formatting
# them here would rewrite content this repo does not own.
.agents
skills-lock.json

pnpm-lock.yaml

# Generated by TanStack Router — never hand-formatted
src/routeTree.gen.ts

# Compiled by Paraglide from messages/ — edit the catalogs, not the output
src/paraglide

# Inlang keeps its cache and lix state here; only settings.json is ours
project.inlang/**
!project.inlang/settings.json

# The evaluation corpus and its manifest: written byte-for-byte by
# `pnpm eval:docs`. Reformatting would move the lines that the manifest,
# questions and citations point at. The generator's own sources stay formatted.
eval/docs/*.md
eval/docs/manifest.json

# Chunker fixtures: byte-exact by design (CRLF endings, an unclosed fence,
# whitespace-only files). Formatting them would destroy what they test.
src/server/modules/ingestion/chunker/__fixtures__
